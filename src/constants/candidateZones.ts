import { CandidateZone } from '../types';

// Inicialización de zonas candidatas (vacío por defecto para mostrar únicamente las capas reales de /CAPAS/)
export const PERU_CANDIDATE_ZONES: CandidateZone[] = [];
